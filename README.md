# Dataset: Kamus Sentimen Kuliner (Converted)

This folder contains CSV conversions of the uploaded Excel files.

Files included:
- `data/7. negative (1).xlsx (kamus hasil).csv` - converted CSV from original Excel (negative).
- `data/8. positive (1).xlsx (kamus hasil).csv` - converted CSV from original Excel (positive).

Notes:
- CSVs saved with `utf-8-sig` encoding to improve compatibility with Excel (Windows).
- Please check `METADATA.md` for column descriptions and data provenance.
- Keep the original `.xlsx` file as an archive if you need multiple sheets or formatting.

Author: Uploaded by user
